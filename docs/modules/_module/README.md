<!--
  模块文档模板 —— 复制整个文件夹为 docs/modules/<module>/,本文件即该模块的 README.md(总览)。
  模块内复杂的子主题再加 docs/modules/<module>/<topic>.md,并在下方「子页」链接。
  登记到 AGENTS.md 导航索引。文件级细节写在源文件文件头,不在这里重复。删掉本注释。
-->

# <模块名>

## 职责
<这个模块整体负责什么。一两句。边界:管什么、不管什么。>

## 文件清单与关系
- `<path/to/a.ts>` — <职责>
- `<path/to/b.ts>` — <职责>
- 调用关系:`<a → b → 外部服务>`

## 数据流
<输入从哪来、经过什么处理、输出到哪去。>

## 对外接口
<暴露给其他模块 / 外部的入口、函数、路由、事件、契约。>

## 注意事项
<坑、约束、不变量、历史包袱。改这块前必须知道的事。>

## 子页(可选,模块内复杂子主题)
- [<topic>](./<topic>.md) — <这个子主题讲什么>
